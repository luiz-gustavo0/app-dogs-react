import React from 'react';
import Feed from '../Feed';

import './styles.css';

const Home = () => {
  return (
    <section className='container main-container'>
      <Feed />
    </section>
  );
};

export default Home;
